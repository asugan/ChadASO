{
  "name": "@aso/desktop",
  "private": true,
  "version": "0.1.0",
  "main": "main.cjs",
  "scripts": {
    "dev": "cross-env-shell NODE_ENV=development RENDERER_URL=http://127.0.0.1:5173 \"wait-on http-get://127.0.0.1:5173 && electron --no-sandbox --disable-setuid-sandbox .\"",
    "start": "electron --no-sandbox --disable-setuid-sandbox ."
  },
  "devDependencies": {
    "electron": "^36.0.0"
  }
}
